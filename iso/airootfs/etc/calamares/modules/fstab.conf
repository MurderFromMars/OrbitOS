# OrbitOS — fstab module
---
mountOptions:
    default: defaults,noatime
    btrfs: defaults,noatime,compress=zstd
    ext4: defaults,noatime
    xfs: defaults,noatime

ssdExtraMountOptions:
    btrfs: discard=async,ssd

crypttabOptions: luks
